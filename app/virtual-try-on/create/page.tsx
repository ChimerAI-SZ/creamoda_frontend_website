import type { Metadata } from 'next';

export { default } from '@/app/(saas)/create/virtual-try-on/page';

export const metadata: Metadata = {
  alternates: {
    canonical: 'https://creamoda.ai/virtual-try-on/create',
  },
};


